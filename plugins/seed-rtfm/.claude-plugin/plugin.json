{
  "name": "seed-rtfm",
  "displayName": "Seed RTFM",
  "description": "Seeds a first-pass RTFM documentation corpus into ProductNow for the current repository",
  "version": "1.0.0",
  "author": {
    "name": "ProductNow"
  },
  "userConfig": {
    "mcp_key": {
      "type": "string",
      "title": "ProductNow MCP key",
      "description": "Optional — paste your ProductNow MCP key from Settings → MCP in the ProductNow app for non-interactive setup. Leave blank to authenticate via OAuth (/mcp) instead.",
      "sensitive": true,
      "required": false
    }
  }
}
